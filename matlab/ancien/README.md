# Anciens fichiers (non utilisés)

Première approche : compléter le modèle `.slx` fourni au départ. Elle est
**abandonnée** : le modèle est désormais construit entièrement par
`../construire_modele.m`, à partir de la logique du firmware. Ces fichiers ne
sont gardés que pour l'historique.

| Fichier | Rôle d'origine |
|---|---|
| `Commande_Sechoir_Hybride.slx` | modèle fourni au départ |
| `Commande_Sechoir_Hybride_corrige.slx` | même modèle, paramètres physiques corrigés |
| `completer_fsm_sechoir.m` | complétait le chart du modèle fourni |
| `preparer_simulation.m` | câblait le modèle fourni pour la simulation |
